const express = require("express");
const router = express.router();
const alunos2 = require("../models/cad.models");
router.use(express.json());

router.post ("/", async function (rec, res) {
    try {
        const 
    }
});